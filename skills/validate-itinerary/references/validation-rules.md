# Validation rules

Block invalid dates, duplicate stable IDs, malformed activities, chronological overlap, and negative costs. Warn when a day has more than five anchors, total estimated activity cost exceeds budget, dynamic facts lack retrieval timestamps, or transfers appear implausibly short. Never interpret an estimate as a confirmed reservation.
